[package]
name = "cmpfs"
version = "0.1.0-dev"
edition = "2024"
description = "Read-only decoder for Apple decmpfs transparent filesystem compression"
license = "MIT"
repository = "https://github.com/Dil4rd/dpp"
keywords = ["decmpfs", "apple", "macos", "compression", "hfs"]
categories = ["compression", "parsing"]

[dependencies]
flate2 = "1.0"
lzfse_rust = "0.2"
lzvn-core = "0.1"
thiserror = "2"
