# Third-party licenses

The `dpp-py` wheels are a compiled Rust extension module, and statically link
the crates below. Their licence texts and copyright notices are reproduced here
as those licences require. `dpp-py` itself is MIT; see `LICENSE`.

This file is generated by `cargo about generate` from `about.toml` and
`about.hbs`. Do not edit it by hand -- CI regenerates it and fails if the
committed copy is stale.

{{#each licenses}}
## {{name}}

Used by:
{{#each used_by}}
- [{{crate.name}} {{crate.version}}]({{#if crate.repository}}{{crate.repository}}{{else}}https://crates.io/crates/{{crate.name}}{{/if}})
{{/each}}

```
{{text}}
```

{{/each}}
