[package]
name = "dpp"
version = "0.5.0-dev"
edition = "2024"
description = "DMG + HFS+/APFS + PKG + PBZX pipeline - walk through Apple disk images to extract packages"
license = "MIT"
repository = "https://github.com/Dil4rd/dpp"
keywords = ["dmg", "apple", "macos", "pkg", "filesystem"]
categories = ["compression", "encoding", "parsing"]

[dependencies]
udif = { version = "0.4.0-dev", path = "../udif" }
pbzx = { version = "0.4.0-dev", path = "../pbzx" }
hfsplus = { version = "0.2.5-dev", path = "../hfsplus" }
apfs = { version = "0.3.0-dev", path = "../apfs" }
xara = { version = "0.4.0-dev", path = "../xara" }
thiserror = "2"
tempfile = "3"

[dev-dependencies]
flate2 = "1.0"
hfsplus = { path = "../hfsplus", features = ["testutil"] }

[features]
default = ["extract"]
extract = []
parallel = ["pbzx/parallel", "udif/parallel"]
