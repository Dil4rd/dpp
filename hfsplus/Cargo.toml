[package]
name = "hfsplus"
version = "0.4.0"
edition = "2024"
description = "Read-only HFS+ / HFSX filesystem parser"
license = "MIT"
repository = "https://github.com/Dil4rd/dpp"
keywords = ["hfs", "hfsplus", "apple", "macos", "filesystem"]
categories = ["parsing", "filesystem"]

[dependencies]
byteorder = "1.5"
cmpfs = { version = "0.1.0", path = "../cmpfs" }
thiserror = "2"

[features]
testutil = []
